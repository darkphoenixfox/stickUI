# ControlPad dependencies
# -----------------------
# Install with:  pip install -r requirements.txt

# GUI framework
PyQt6>=6.4.0

# TOML parsing (only needed for Python < 3.11)
# tomli>=2.0.0

# Image manipulation (optional – used for background image pre-processing)
Pillow>=10.0.0